services:
  quiz:
    build: .
    image: renmay-quiz:latest
    container_name: renmay-quiz
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
