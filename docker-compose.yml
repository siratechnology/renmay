services:
  quiz:
    build: .
    image: renmay-quiz:latest
    container_name: renmay-quiz
    ports:
      - "4100:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
